<template>
  <!-- Write your awesome HTML here! (Note: Needs a single root element) -->
  <div />
</template>

<script lang="ts">
/* eslint-disable max-len */

// Imports.
// import HelloWorld from '@/app/HelloWorld.vue';
// Note: @ is an alias to /src
import { Component, Vue } from 'vue-property-decorator';

/* eslint-enable max-len */

// Lazy load imports.

// Lazy load services.

// Lazy load components.

// Static references and methods.
// Note: This is outside the class because every static reference inside a vue component is stripped
// out when compiled to javascript.

// Vue Components.
@Component({
  components: {},
})
export default class Template extends Vue {
  // Props.
  // Refs.
  // Data.
  // Computed properties (written as getters).
  // Custom methods.
  // Watchers.
  // Hooks (lifecycle, custom, etc...).
}
</script>
